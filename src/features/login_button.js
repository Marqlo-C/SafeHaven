/**
 * LoginButtonFeature — Discrete login access button.
 *
 * Enables a small, subtle orange button component that appears on disguised
 * app pages to provide hidden access to the login page. The button is
 * intentionally unlabeled and low-contrast to avoid drawing attention.
 */

class LoginButtonFeature {
  static init() {
    console.log('[LoginButtonFeature] Discrete login button enabled.');
  }
}

module.exports = { LoginButtonFeature };
